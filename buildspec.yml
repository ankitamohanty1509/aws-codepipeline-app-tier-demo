version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Installing dependencies...
      - npm install
      
artifacts:
  files:
    - '**/*'  # Include all files in the repository
  base-directory: 'app-tier'  # This should be the directory with the app code